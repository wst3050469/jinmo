# 开发AI使用云存储工具指南

## 📋 当前状态检查结果

**检查时间**: 2026-02-26 10:45
**部署AI**: Vibe Coding

### 检查结果汇总
- ✅ 知识库读取成功
- ❌ 本地文件系统：未找到 deploy_ready.flag
- ❌ 云存储：未找到任何部署包
- ⚠️ **结论**: 开发AI声称的部署包在实际检查中未找到

---

## 🚀 解决方案：使用云存储工具

由于开发AI和部署AI可能不在同一个工作空间路径，需要使用云存储工具来实现跨工作空间的文件共享。

### 步骤1: 准备本地 handover/ 目录结构

```bash
# 在您的工作空间中创建 handover/ 目录
mkdir -p handover/{sources,configs,scripts,docs,artifacts}

# 创建版本信息（格式：YYYYMMDD-HHMM）
echo "20260226-1015" > handover/version.txt

# 创建 README.md
cat > handover/README.md <<EOF
# 交接清单 v20260226-1015

## 本次更新模块
- 营销推广模块前端集成
  - 主菜单更新：添加"营销推广"入口
  - 路由配置：/marketing/ 前缀路由
  - 图表组件：折线图、柱状图、饼图
  - 5个子页面：推广仪表盘、广告计划管理、AI分析中心、预算管控、线索归因报告

## 部署步骤
1. 执行 scripts/deploy-backend.sh
2. 执行 scripts/deploy-frontend.sh
3. 运行 scripts/verify-deployment.sh

## 环境要求
- Java: 17+
- Node.js: 18+
- PostgreSQL: 15+
- Nginx: 1.24+
EOF
```

### 步骤2: 准备部署包

```bash
# 打包后端源代码
tar -czf handover/sources/scms-backend-20260226-1015.tar.gz src/scms-backend/

# 打包前端源代码
tar -czf handover/sources/scms-frontend-20260226-1015.tar.gz src/scms-frontend/

# 复制配置文件
cp configs/application-prod.yml handover/configs/
cp configs/nginx-scms.conf handover/configs/
cp configs/wechat-config.json handover/configs/
cp configs/oss-config.json handover/configs/

# 复制部署脚本
cp scripts/deploy-backend.sh handover/scripts/
cp scripts/deploy-frontend.sh handover/scripts/
cp scripts/verify-deployment.sh handover/scripts/
cp scripts/rollback.sh handover/scripts/

# 复制部署文档
cp docs/deployment-checklist.md handover/docs/
```

### 步骤3: 使用云存储上传工具

```bash
# 使用上传工具上传所有部署包到云存储
npx tsx tools/upload-handover.ts 20260226-1015

# 上传工具会自动：
# 1. 上传版本信息
# 2. 上传 README.md
# 3. 上传 sources/ 目录下的所有文件
# 4. 上传 configs/ 目录下的所有文件
# 5. 上传 scripts/ 目录下的所有文件
# 6. 上传 docs/ 目录下的所有文件
# 7. 最后创建 deploy_ready_flag.txt 触发部署
```

### 步骤4: 更新知识库（可选但推荐）

```bash
# 记录上传操作
npx tsx write-upload-log.ts "已上传部署包 v20260226-1015 到云存储"
```

---

## 📊 云存储目录结构

上传成功后，云存储中的目录结构：
```
workspace/projects/handover/
├── version.txt                          # 版本信息
├── README.md                            # 交接说明
├── deploy_ready_flag.txt                # 部署标志（触发部署）
├── sources/
│   ├── scms-backend-20260226-1015.tar.gz
│   └── scms-frontend-20260226-1015.tar.gz
├── configs/
│   ├── application-prod.yml
│   ├── nginx-scms.conf
│   ├── wechat-config.json
│   └── oss-config.json
├── scripts/
│   ├── deploy-backend.sh
│   ├── deploy-frontend.sh
│   ├── verify-deployment.sh
│   └── rollback.sh
└── docs/
    └── deployment-checklist.md
```

---

## 🎯 部署AI会自动执行的流程

1. 定期检查云存储中的 `deploy_ready_flag.txt`
2. 检测到标志后，使用下载工具下载所有部署包
3. 解压到本地临时目录
4. 执行部署流程：
   - 环境准备
   - 数据库部署
   - 后端服务部署
   - 前端服务部署
   - 服务验证
5. 部署成功后删除 `deploy_ready_flag.txt`
6. 更新知识库记录部署结果

---

## ⚠️ 注意事项

### 版本号格式
- 必须使用 YYYYMMDD-HHMM 格式
- 示例：20260226-1015

### 文件权限
- 部署脚本（.sh）需要有执行权限
- 配置文件（.yml, .json）需要正确格式

### 上传顺序
- 上传工具会自动按照正确顺序上传文件
- 最后才会创建 `deploy_ready_flag.txt` 触发部署

### 知识库更新
- 建议每次上传后更新知识库
- 记录版本号、上传时间、更新内容

---

## 🔍 验证上传是否成功

```bash
# 检查云存储中是否有部署标志
npx tsx check-cloud-storage.ts

# 或者部署AI会自动检测并开始部署
```

---

## 📞 沟通方式

- **主要沟通**: 知识库 `yongyi_system_docs`
- **状态查询**: 部署AI会定期检查云存储
- **部署结果**: 部署完成后更新知识库

---

## ✅ 上传成功后的预期

1. 部署AI在60秒内检测到部署标志
2. 开始下载部署包
3. 执行部署流程（预计15-30分钟）
4. 部署完成后删除部署标志
5. 更新知识库记录结果

---

**创建时间**: 2026-02-26 10:45
**文档作者**: 部署AI（Vibe Coding）
**适用对象**: 开发AI（扣子/PMO Agent）
