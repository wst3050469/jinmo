# SCMS项目：Git仓库协同方案（技术详细版）

## 文档信息
- **版本**: v1.0
- **创建日期**: 2026-02-26
- **技术栈**: Git + SSH密钥
- **数据集**: yongyi_system_docs

---

## 🎯 方案概述

### 核心原则
1. **单一事实来源**: 所有代码存储在Git仓库
2. **版本控制**: 每次部署都有版本记录
3. **可追溯性**: 所有问题都可以追溯到具体代码版本
4. **简单高效**: 使用Git标准流程，无需额外工具

### 架构图
```
┌─────────────────┐
│  开发AI（扣子）  │
│  - 开发代码      │
│  - 提交到Git    │
└────────┬────────┘
         │ git push
         ↓
┌─────────────────┐
│   Git仓库        │
│  (GitHub/GitLab)│
│  - 存储代码      │
│  - 版本历史      │
└────────┬────────┘
         │ git pull
         ↓
┌─────────────────┐
│  部署AI（Vibe）  │
│  - 下载代码      │
│  - 部署到服务器  │
└────────┬────────┘
         │ scp/rsync
         ↓
┌─────────────────┐
│  生产服务器      │
│  120.55.5.220   │
└─────────────────┘
```

---

## 📋 前置条件

### 1. Git仓库要求
- **托管平台**: GitHub / GitLab / Gitee（三选一）
- **仓库类型**: Private（私有仓库）
- **访问协议**: HTTPS（推荐）或SSH
- **分支策略**: main（主分支）用于生产部署

### 2. 访问权限配置
- **开发AI**: Write权限（提交代码）
- **部署AI**: Read权限（下载代码）

### 3. Git仓库目录结构
```
scms-project/
├── backend/                    # Spring Boot后端
│   ├── src/
│   ├── pom.xml
│   └── application.yml
├── frontend/                   # Vue 3前端
│   ├── src/
│   ├── package.json
│   └── vite.config.ts
├── nginx/                      # Nginx配置
│   └── nginx.conf
├── scripts/                    # 部署脚本
│   ├── deploy-backend.sh
│   └── deploy-frontend.sh
├── README.md
└── .gitignore
```

---

## 🚀 阶段一：初始化Git仓库

### 步骤1：获取Git仓库地址
**用户操作**:
1. 在GitHub/GitLab创建仓库
2. 复制仓库地址（HTTPS格式）
3. 提供给部署AI

**示例地址**:
```
https://github.com/your-username/scms-project.git
```

### 步骤2：部署AI初始化仓库
**执行脚本**: `scripts/init-git-repo.sh`

```bash
#!/bin/bash

# Git仓库地址（用户提供）
GIT_REPO_URL="https://github.com/your-username/scms-project.git"

# 配置Git用户信息
git config --global user.name "SCMS Deploy AI"
git config --global user.email "deploy@scms.com"

# 初始化仓库
git init

# 创建.gitignore
cat > .gitignore << 'EOF'
# 编译输出
target/
dist/
node_modules/

# IDE
.idea/
.vscode/

# 日志
logs/
*.log

# 临时文件
*.tmp
*.temp

# 敏感信息（不要提交到Git）
application-prod.yml
.env
*.pem
EOF

# 添加所有文件
git add .

# 创建初始提交
git commit -m "chore: 初始化SCMS项目代码库"

# 添加远程仓库
git remote add origin $GIT_REPO_URL

# 推送到远程仓库
git push -u origin main

echo "✅ Git仓库初始化完成！"
echo "📍 仓库地址: $GIT_REPO_URL"
```

### 步骤3：验证初始化
```bash
# 查看远程仓库
git remote -v

# 查看提交历史
git log --oneline
```

---

## 🔄 阶段二：日常协同流程

### 场景1：开发AI提交新代码

#### 开发AI操作流程

**1. 准备代码**
```bash
# 进入项目目录
cd /path/to/scms-project

# 查看修改的文件
git status

# 添加修改的文件
git add .

# 查看暂存的修改
git diff --staged
```

**2. 提交代码**
```bash
# 提交代码（必须有明确的提交信息）
git commit -m "feat: 完成营销推广模块开发

- 新增营销活动管理功能
- 新增推广效果统计报表
- 优化前端页面性能"

# 推送到远程仓库
git push
```

**3. 创建版本标签（可选）**
```bash
# 为当前提交打标签
git tag -a v1.0.1 -m "营销推广模块v1.0.1"

# 推送标签到远程
git push origin v1.0.1
```

**4. 通知部署AI**
```
【部署请求】
仓库: scms-project
分支: main
提交: abc123def456
版本: v1.0.1（可选）
说明: 完成营销推广模块开发
```

### 场景2：部署AI接收部署请求

#### 部署AI操作流程

**1. 下载最新代码**
```bash
# 进入项目目录
cd /workspace/projects

# 拉取最新代码
git pull origin main

# 查看最新提交
git log -1 --pretty=format:"%h - %s (%cd)"
```

**2. 验证代码**
```bash
# 检查文件完整性
git status

# 查看最新修改
git log -5 --oneline

# 验证特定版本的代码（如果有标签）
git show v1.0.1 --stat
```

**3. 部署到服务器**
```bash
# 使用部署脚本
bash scripts/deploy-backend.sh
bash scripts/deploy-frontend.sh
```

**4. 记录部署日志**
```bash
# 记录部署信息
echo "[$(date '+%Y-%m-%d %H:%M:%S')] 部署版本: $(git log -1 --pretty=format:'%h')" >> deploy.log
```

---

## 📦 阶段三：版本管理

### 版本标签规范

#### 语义化版本号
```
v主版本号.次版本号.修订号

示例：
v1.0.0    - 初始版本
v1.0.1    - 修复bug（修订号+1）
v1.1.0    - 新增功能（次版本号+1）
v2.0.0    - 重大更新（主版本号+1）
```

#### 创建版本标签
```bash
# 轻量标签
git tag v1.0.1

# 附注标签（推荐）
git tag -a v1.0.1 -m "营销推广模块v1.0.1

- 新增营销活动管理
- 新增推广效果统计"
```

#### 查看版本标签
```bash
# 列出所有标签
git tag -l

# 查看标签详情
git show v1.0.1

# 按时间排序查看标签
git tag -l --sort=-version:refname
```

### 版本回滚

#### 如果新版本有问题，回滚到之前的版本
```bash
# 查看历史提交
git log --oneline

# 回滚到指定版本（创建新提交）
git revert abc123def456

# 或者硬回滚（谨慎使用）
git reset --hard abc123def456
git push -f origin main
```

---

## 🔍 阶段四：代码审查

### 查看代码变更

#### 查看文件修改差异
```bash
# 查看工作区修改
git diff

# 查看暂存区修改
git diff --staged

# 查看两次提交的差异
git diff abc123def456 def456ghi789

# 查看某个文件的修改历史
git log --follow -- filename.txt
```

#### 查看提交详情
```bash
# 查看提交详情
git show abc123def456

# 查看最近5次提交
git log -5 --pretty=format:"%h - %s (%an, %ar)"

# 查看统计信息
git log --stat
```

---

## ⚙️ 阶段五：自动化脚本

### 脚本1：开发AI提交助手
**文件**: `scripts/git-commit-helper.sh`

```bash
#!/bin/bash

# Git提交助手脚本

echo "🚀 开始准备提交代码..."

# 1. 查看修改的文件
echo "📋 修改的文件："
git status --short

# 2. 输入提交信息
read -p "请输入提交信息: " COMMIT_MSG

# 3. 添加所有文件
git add .

# 4. 提交代码
git commit -m "$COMMIT_MSG"

# 5. 推送到远程仓库
git push

# 6. 输出提交信息
echo "✅ 代码已提交！"
echo "📍 提交信息: $COMMIT_MSG"
echo "📅 提交时间: $(date '+%Y-%m-%d %H:%M:%S')"
echo "🔗 提交哈希: $(git log -1 --pretty=format:'%h')"
```

### 脚本2：部署AI下载助手
**文件**: `scripts/git-pull-helper.sh`

```bash
#!/bin/bash

# Git下载助手脚本

echo "🚀 开始下载最新代码..."

# 1. 记录当前版本
BEFORE_COMMIT=$(git log -1 --pretty=format:'%h')
echo "📍 当前版本: $BEFORE_COMMIT"

# 2. 拉取最新代码
git pull origin main

# 3. 记录新版本
AFTER_COMMIT=$(git log -1 --pretty=format:'%h')
echo "📍 最新版本: $AFTER_COMMIT"

# 4. 查看变更
echo "📋 本次变更："
git log $BEFORE_COMMIT..$AFTER_COMMIT --oneline

# 5. 生成部署信息
cat > deploy-info.json << EOF
{
  "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
  "before_commit": "$BEFORE_COMMIT",
  "after_commit": "$AFTER_COMMIT",
  "changes": $(git log $BEFORE_COMMIT..$AFTER_COMMIT --oneline | wc -l)
}
EOF

echo "✅ 代码已下载！"
echo "📦 部署信息: deploy-info.json"
```

### 脚本3：版本对比工具
**文件**: `scripts/git-diff-helper.sh`

```bash
#!/bin/bash

# Git版本对比工具

# 检查参数
if [ $# -ne 2 ]; then
  echo "用法: $0 <版本1> <版本2>"
  echo "示例: $0 v1.0.0 v1.0.1"
  exit 1
fi

VERSION1=$1
VERSION2=$2

echo "🔍 对比版本: $VERSION1 -> $VERSION2"
echo "================================"

# 显示版本信息
echo "📌 版本 $VERSION1 信息："
git show $VERSION1 --no-patch

echo ""
echo "📌 版本 $VERSION2 信息："
git show $VERSION2 --no-patch

echo ""
echo "📊 文件变更统计："
git diff --stat $VERSION1 $VERSION2

echo ""
echo "📝 详细变更："
git diff $VERSION1 $VERSION2
```

---

## 📊 阶段六：监控与日志

### 部署日志记录
**文件**: `scripts/record-deploy.sh`

```bash
#!/bin/bash

# 记录部署日志

# 部署信息
DEPLOY_TIME=$(date '+%Y-%m-%d %H:%M:%S')
GIT_COMMIT=$(git log -1 --pretty=format:'%h')
GIT_MESSAGE=$(git log -1 --pretty=format:'%s')
GIT_AUTHOR=$(git log -1 --pretty=format:'%an')

# 写入日志
cat >> /app/work/logs/bypass/deploy.log << EOF
================================
部署时间: $DEPLOY_TIME
Git提交: $GIT_COMMIT
提交信息: $GIT_MESSAGE
提交作者: $GIT_AUTHOR
部署状态: 开始
================================
EOF

# 执行部署命令...

# 更新部署状态
cat >> /app/work/logs/bypass/deploy.log << EOF
[$(date '+%Y-%m-%d %H:%M:%S')] 部署状态: 成功
================================
EOF
```

---

## 🛡️ 安全与备份

### 敏感文件保护

**.gitignore 配置**:
```gitignore
# 敏感配置文件
application-prod.yml
.env
*.pem
*.key

# 数据库备份
*.sql
*.dump

# 临时文件
*.tmp
*.temp
*.log

# 编译输出
target/
dist/
node_modules/
```

### 定期备份策略

```bash
# 备份Git仓库到服务器
rsync -avz /workspace/projects/.git user@120.55.5.220:/backup/scms-git/

# 备份到云存储（可选）
tar -czf scms-backup-$(date +%Y%m%d).tar.gz .git
# 上传到云存储
```

---

## 📞 故障排查

### 常见问题

#### 1. 拉取失败
```bash
# 错误: "error: Your local changes to the following files would be overwritten by merge"
# 解决: 先提交本地修改
git add .
git commit -m "临时保存本地修改"
git pull
```

#### 2. 推送失败
```bash
# 错误: "error: failed to push some refs to..."
# 解决: 先拉取最新代码
git pull --rebase
git push
```

#### 3. 权限被拒绝
```bash
# 错误: "Permission denied (publickey)"
# 解决: 检查SSH密钥配置
ssh-keygen -t ed25519 -C "your_email@example.com"
cat ~/.ssh/id_ed25519.pub
# 添加到GitHub的SSH Keys
```

---

## ✅ 检查清单

### 初始化阶段
- [ ] 用户创建Git仓库
- [ ] 提供仓库地址给部署AI
- [ ] 部署AI初始化仓库
- [ ] 上传现有代码
- [ ] 验证代码完整性

### 日常开发
- [ ] 开发AI提交代码
- [ ] 创建版本标签
- [ ] 通知部署AI
- [ ] 部署AI下载代码
- [ ] 部署到服务器
- [ ] 记录部署日志

### 版本管理
- [ ] 使用语义化版本号
- [ ] 为每次发布创建标签
- [ ] 记录版本变更日志
- [ ] 定期备份Git仓库

---

## 📚 参考文档

- [Git官方文档](https://git-scm.com/doc)
- [GitHub使用指南](https://docs.github.com/)
- [语义化版本规范](https://semver.org/lang/zh-CN/)

---

## 🎯 总结

**Git协同方案的优势**:
1. ✅ 简单易用：符合行业标准
2. ✅ 版本控制：完整的历史记录
3. ✅ 可追溯性：所有修改都有记录
4. ✅ 安全可靠：云端备份，不会丢失
5. ✅ 高效协同：开发AI和部署AI分工明确

**操作流程**:
1. 用户创建Git仓库 → 提供地址
2. 部署AI初始化仓库 → 上传代码
3. 开发AI提交代码 → 创建标签 → 通知部署AI
4. 部署AI下载代码 → 部署 → 记录日志

---

**方案版本**: v1.0
**创建日期**: 2026-02-26
**数据集**: yongyi_system_docs
**状态**: ✅ 等待用户创建Git仓库并提供地址
