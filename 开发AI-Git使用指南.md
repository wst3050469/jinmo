# 开发AI（扣子/PMO Agent）Git使用指南

## 📋 文档信息

- **适用对象**: 开发AI（扣子/PMO Agent）
- **版本**: v1.0
- **创建日期**: 2026-02-26
- **仓库地址**: https://github.com/wst3050469/jinmo
- **SSH地址**: git@github.com:wst3050469/jinmo.git

---

## 🎯 工作流程概述

```
┌─────────────────┐
│  开发AI（你）    │
├─────────────────┤
│ 1. 克隆仓库     │
│ 2. 开发代码     │
│ 3. 提交代码     │
│ 4. 创建标签     │
│ 5. 通知部署AI   │
└────────┬────────┘
         │ git push
         ↓
┌─────────────────┐
│  Git仓库        │
│  jinmo          │
└────────┬────────┘
         │ 通知
         ↓
┌─────────────────┐
│  部署AI         │
│  下载并部署     │
└─────────────────┘
```

---

## 🚀 第一步：环境准备

### 1. 检查Git是否安装

```bash
git --version
```

如果没有安装，先安装Git：
```bash
# Ubuntu/Debian
sudo apt-get install git

# CentOS/RHEL
sudo yum install git

# macOS
brew install git
```

### 2. 配置Git用户信息

```bash
git config --global user.name "SCMS Dev AI"
git config --global user.email "dev@scms.com"
```

### 3. 配置SSH密钥（如果还没有）

```bash
# 生成SSH密钥
ssh-keygen -t ed25519 -C "dev@scms.com" -f ~/.ssh/id_ed25519 -N ""

# 添加到SSH代理
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# 复制公钥
cat ~/.ssh/id_ed25519.pub
```

然后访问 https://github.com/wst3050469/jinmo/settings/keys 添加SSH密钥。

---

## 📥 第二步：克隆仓库

### 首次克隆

```bash
# 克隆仓库
git clone git@github.com:wst3050469/jinmo.git

# 进入项目目录
cd jinmo
```

### 查看仓库信息

```bash
# 查看远程仓库地址
git remote -v

# 查看当前分支
git branch

# 查看最近提交
git log --oneline -5
```

---

## 💻 第三步：开发代码

### 创建功能分支（推荐）

```bash
# 创建并切换到功能分支
git checkout -b feature/marketing-module

# 或者从最新代码创建分支
git checkout main
git pull origin main
git checkout -b feature/marketing-module
```

### 查看当前状态

```bash
# 查看修改的文件
git status

# 查看具体修改
git diff

# 查看暂存区的修改
git diff --staged
```

---

## 📤 第四步：提交代码

### 标准提交流程

```bash
# 1. 查看修改
git status

# 2. 添加所有修改的文件
git add .

# 或者只添加特定文件
git add src/main/java/com/jinmo/marketing/*.java

# 3. 查看暂存的修改
git diff --staged

# 4. 提交代码（必须有清晰的提交信息）
git commit -m "feat: 完成营销推广模块开发

- 新增营销活动管理功能
- 新增推广效果统计报表
- 优化前端页面性能
- 修复数据查询bug"

# 5. 推送到远程仓库
git push origin feature/marketing-module
```

### 提交信息格式规范

使用语义化提交信息（Conventional Commits）：

```
<类型>: <简短描述>

<详细描述（可选）>

<关联Issue（可选）>
```

#### 类型说明

| 类型 | 说明 | 示例 |
|------|------|------|
| feat | 新增功能 | feat: 新增营销活动管理 |
| fix | 修复bug | fix: 修复数据查询超时问题 |
| docs | 文档更新 | docs: 更新部署指南 |
| style | 代码格式 | style: 统一代码缩进 |
| refactor | 代码重构 | refactor: 重构用户认证模块 |
| test | 测试相关 | test: 添加单元测试 |
| chore | 构建/工具 | chore: 更新依赖版本 |

#### 提交信息示例

**好的提交信息**：
```
feat: 完成营销推广模块开发

- 新增营销活动管理功能
- 新增推广效果统计报表
- 优化前端页面性能

Closes #123
```

**不好的提交信息**：
```
update
fix bug
完成开发
```

---

## 🏷️ 第五步：创建版本标签

### 何时创建版本标签

- **必须创建**：每次发布新版本
- **建议创建**：重要的功能更新、bug修复

### 创建版本标签

```bash
# 查看最新提交
git log -1

# 创建附注标签（推荐）
git tag -a v1.0.1 -m "营销推广模块v1.0.1

- 新增营销活动管理
- 新增推广效果统计
- 优化前端性能"

# 推送标签到远程仓库
git push origin v1.0.1
```

### 版本号规范

```
v主版本号.次版本号.修订号

示例：
v1.0.0 - 初始版本
v1.0.1 - 修复bug（修订号+1）
v1.1.0 - 新增功能（次版本号+1）
v2.0.0 - 重大更新（主版本号+1）
```

### 查看所有标签

```bash
# 列出所有标签
git tag -l

# 按时间排序查看
git tag -l --sort=-version:refname

# 查看标签详情
git show v1.0.1
```

---

## 🔔 第六步：通知部署AI

### 通知方式

通过知识库 `yongyi_system_docs` 标记 `[部署]` 通知部署AI。

### 通知内容模板

```
【部署请求】

项目: SCMS施工综合管理系统
仓库: git@github.com:wst3050469/jinmo.git
分支: feature/marketing-module（或main）
提交: abc123def456
版本: v1.0.1
提交信息: feat: 完成营销推广模块开发
提交时间: 2026-02-26 10:30:00

变更内容：
- 新增营销活动管理功能
- 新增推广效果统计报表
- 优化前端页面性能

需要部署AI执行：
1. 切换到main分支
2. 合并feature/marketing-module分支
3. 部署到生产服务器
4. 验证功能
5. 反馈部署结果
```

### 注意事项

- **提供完整的提交信息**：包括提交哈希、分支名、版本号
- **说明变更内容**：让部署AI了解这次更新了什么
- **注明特殊要求**：如果需要特定的部署步骤，提前说明

---

## 🛠️ 使用辅助脚本

### git-commit-helper.sh

如果环境中有这个脚本，可以使用它简化提交流程：

```bash
# 运行提交助手
bash scripts/git-commit-helper.sh

# 按照提示操作：
# 1. 查看修改的文件
# 2. 输入提交信息
# 3. 自动提交和推送
# 4. 生成部署通知
```

### 手动提交流程（完整版）

```bash
# 1. 拉取最新代码
git pull origin main

# 2. 创建功能分支
git checkout -b feature/new-feature

# 3. 开发代码...

# 4. 查看修改
git status

# 5. 添加文件
git add .

# 6. 提交代码
git commit -m "feat: 新功能描述"

# 7. 推送分支
git push origin feature/new-feature

# 8. 合并到main分支
git checkout main
git pull origin main
git merge feature/new-feature

# 9. 推送main分支
git push origin main

# 10. 创建版本标签
git tag -a v1.0.1 -m "版本说明"
git push origin v1.0.1

# 11. 通知部署AI
# 通过知识库标记 [部署]
```

---

## 📊 分支管理策略

### 分支类型

| 分支类型 | 命名规则 | 用途 | 生命周期 |
|---------|---------|------|---------|
| main | main | 生产环境代码 | 永久 |
| develop | develop | 开发环境代码 | 永久 |
| feature | feature/xxx | 新功能开发 | 临时 |
| bugfix | bugfix/xxx | 修复bug | 临时 |
| release | release/x.x.x | 发布准备 | 临时 |
| hotfix | hotfix/x.x.x | 紧急修复 | 临时 |

### 分支工作流程

```
main (生产)
  ↓
develop (开发)
  ↓
feature/new-feature (开发新功能)
  ↓
develop (合并)
  ↓
release/1.0.0 (发布准备)
  ↓
main (发布)
```

### 简化版工作流程（推荐）

如果团队规模小，可以使用简化版：

```
main (生产)
  ↓
feature/new-feature (开发新功能)
  ↓
main (合并并发布)
```

---

## 🎯 最佳实践

### 1. 频繁提交

```bash
# ❌ 不好的做法：开发了一周才提交一次
git add .
git commit -m "完成了一周的开发"

# ✅ 好的做法：每个功能完成后立即提交
git add .
git commit -m "feat: 完成用户登录功能"

git add .
git commit -m "feat: 完成用户注册功能"
```

### 2. 清晰的提交信息

```bash
# ❌ 不好的做法
git commit -m "update"
git commit -m "fix bug"
git commit -m "完成"

# ✅ 好的做法
git commit -m "feat: 新增用户登录功能"
git commit -m "fix: 修复数据查询超时问题"
git commit -m "docs: 更新API文档"
```

### 3. 提交前检查

```bash
# 提交前一定要检查
git status        # 查看修改的文件
git diff          # 查看具体修改
git diff --staged # 查看暂存的修改
```

### 4. 使用.gitignore

确保敏感文件不会被提交：

```gitignore
# 敏感配置
application-prod.yml
.env
*.pem

# 临时文件
*.tmp
*.log

# 编译输出
target/
dist/
node_modules/
```

### 5. 定期同步

```bash
# 每天开始工作前，先拉取最新代码
git pull origin main

# 推送前，先拉取再推送
git pull origin main
git push origin main
```

---

## 🔍 查看历史和变更

### 查看提交历史

```bash
# 查看最近10次提交
git log --oneline -10

# 查看提交详情
git log -1 --pretty=format:"%h - %s (%an, %ar)"

# 查看某个提交的详情
git show abc123def456

# 查看文件修改历史
git log --follow -- filename.txt
```

### 对比版本

```bash
# 对比两个提交
git diff abc123def456 def456ghi789

# 对比两个版本标签
git diff v1.0.0 v1.0.1

# 对比当前版本和上个版本
git diff HEAD~1 HEAD

# 只看文件列表
git diff --name-only v1.0.0 v1.0.1

# 看统计信息
git diff --stat v1.0.0 v1.0.1
```

### 撤销修改

```bash
# 撤销工作区的修改
git restore filename.txt

# 撤销暂存区的修改
git restore --staged filename.txt

# 撤销最后一次提交（保留修改）
git reset --soft HEAD~1

# 撤销最后一次提交（删除修改，谨慎使用）
git reset --hard HEAD~1

# 回滚到某个版本
git reset --hard v1.0.0
```

---

## ❓ 常见问题

### Q1: 推送失败，提示"Permission denied"

**原因**: 没有仓库访问权限或SSH密钥配置错误

**解决**:
1. 检查SSH密钥：`ssh -T git@github.com`
2. 检查仓库权限：访问GitHub设置页面
3. 重新添加SSH密钥

### Q2: 拉取失败，提示"Your local changes would be overwritten"

**原因**: 本地有未提交的修改，与远程代码冲突

**解决**:
```bash
# 方案1：先提交本地修改
git add .
git commit -m "临时保存"
git pull origin main

# 方案2：暂存本地修改
git stash
git pull origin main
git stash pop
```

### Q3: 推送失败，提示"Updates were rejected"

**原因**: 远程有新的提交，本地没有

**解决**:
```bash
# 先拉取远程更新
git pull origin main

# 如果有冲突，解决冲突后
git add .
git commit -m "解决冲突"
git push origin main
```

### Q4: 提交了敏感文件怎么办

**解决**:
```bash
# 1. 从Git中删除文件（但保留本地）
git rm --cached application-prod.yml

# 2. 提交删除
git commit -m "chore: 删除敏感文件"

# 3. 推送到远程
git push origin main

# 4. 添加到.gitignore
echo "application-prod.yml" >> .gitignore
git add .gitignore
git commit -m "chore: 添加.gitignore"
git push origin main
```

### Q5: 如何恢复到之前的版本

**解决**:
```bash
# 方案1：创建新提交回滚（推荐）
git revert v1.0.1
git push origin main

# 方案2：硬回滚（谨慎使用）
git reset --hard v1.0.0
git push -f origin main
```

### Q6: 如何查看某个版本的代码

**解决**:
```bash
# 查看版本详情
git show v1.0.1

# 查看版本中的文件
git show v1.0.1:src/main/java/Main.java

# 切换到该版本（只读）
git checkout v1.0.1

# 切换回最新版本
git checkout main
```

---

## 📋 完整工作流程示例

### 开发新功能

```bash
# 1. 拉取最新代码
git pull origin main

# 2. 创建功能分支
git checkout -b feature/marketing-module

# 3. 开发代码...
# （编写营销推广模块代码）

# 4. 查看修改
git status
git diff

# 5. 提交代码
git add .
git commit -m "feat: 完成营销推广模块开发

- 新增营销活动管理功能
- 新增推广效果统计报表
- 优化前端页面性能"

# 6. 推送分支
git push origin feature/marketing-module

# 7. 合并到main分支
git checkout main
git pull origin main
git merge feature/marketing-module

# 8. 推送main分支
git push origin main

# 9. 创建版本标签
git tag -a v1.0.1 -m "营销推广模块v1.0.1"
git push origin v1.0.1

# 10. 删除功能分支（可选）
git branch -d feature/marketing-module
git push origin --delete feature/marketing-module

# 11. 通知部署AI
# 通过知识库标记 [部署]
```

### 修复Bug

```bash
# 1. 拉取最新代码
git pull origin main

# 2. 创建bug修复分支
git checkout -b bugfix/login-timeout

# 3. 修复bug...
# （修复登录超时问题）

# 4. 提交代码
git add .
git commit -m "fix: 修复登录超时问题

- 增加连接超时时间
- 优化重试机制"

# 5. 推送分支
git push origin bugfix/login-timeout

# 6. 合并到main分支
git checkout main
git pull origin main
git merge bugfix/login-timeout
git push origin main

# 7. 创建版本标签
git tag -a v1.0.2 -m "修复登录超时问题"
git push origin v1.0.2

# 8. 通知部署AI
# 通过知识库标记 [部署]
```

---

## 📚 参考资源

- [Git官方文档](https://git-scm.com/doc)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [语义化版本规范](https://semver.org/lang/zh-CN/)
- [GitHub使用指南](https://docs.github.com/)

---

## 📞 联系方式

- **GitHub仓库**: https://github.com/wst3050469/jinmo
- **知识库**: `yongyi_system_docs`
- **部署通知**: 知识库标记 `[部署]`

---

## ✅ 检查清单

### 开发前
- [ ] 拉取最新代码：`git pull origin main`
- [ ] 创建功能分支：`git checkout -b feature/xxx`
- [ ] 查看当前状态：`git status`

### 提交前
- [ ] 检查修改的文件：`git status`
- [ ] 查看具体修改：`git diff`
- [ ] 编写清晰的提交信息
- [ ] 添加.gitignore中的文件

### 推送前
- [ ] 确认提交信息正确
- [ ] 拉取远程更新：`git pull origin main`
- [ ] 解决冲突（如果有）

### 发布前
- [ ] 创建版本标签：`git tag -a v1.0.1 -m "说明"`
- [ ] 推送标签：`git push origin v1.0.1`
- [ ] 通知部署AI

---

**版本**: v1.0
**创建日期**: 2026-02-26
**适用对象**: 开发AI（扣子/PMO Agent）
**文档类型**: dev-ai-git-guide
